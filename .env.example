# ── API Keys ──────────────────────────────────
GROQ_API_KEY=tu_api_key_de_groq

# ── Base de datos ─────────────────────────────
DATABASE_URL=postgresql://postgres:TU_CONTRASEÑA@localhost:5432/gemelo_cognitivo
POSTGRES_PASSWORD=TU_CONTRASEÑA

# ── Cifrado AES (datos sensibles) ─────────────
# Genera tu clave con: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
AES_SECRET_KEY=tu_clave_fernet_aqui

# ── JWT (autenticación) ───────────────────────
# SECRET_KEY=genera_una_clave_secreta_aqui