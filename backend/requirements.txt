# ============================================================
# Gemelo Cognitivo — Backend Dependencies
# ============================================================
# Install: pip install -r backend/requirements.txt
# ============================================================

# ── Core API ───────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic[email]>=2.0.0
python-dotenv>=1.0.0

# ── Security (auth, password hashing, AES) ─────
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
cryptography>=42.0.0

# ── Database (PostgreSQL + ORM + migrations) ───
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# ── LLM Agents (pipeline orchestration) ───────
langchain>=0.2.0
langgraph>=0.2.0

# ── RAG (vector store + embeddings) ────────────
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ── ML (classifier + serialization + data) ────
scikit-learn>=1.4.0
joblib>=1.3.0
pandas>=2.2.0

# ── Testing ────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0
